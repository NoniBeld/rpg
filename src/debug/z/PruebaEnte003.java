package debug.z;

import com.rpg.ente.Creador;
import com.rpg.ente.Ente;
import com.rpg.ente.Funcion;

import herramientas.texto.Narrador;

public class PruebaEnte003 {/*
    public static void main(String[] args) {
        Ente azul = Creador.obtenerInstancia().crearNuevoEnte("Slime Azul", Funcion.RECEPTOR);
        Ente rojo = Creador.obtenerInstancia().crearNuevoEnte("Slime Rojo", Funcion.ALIMENTO);

        // Los separamos en el espacio
        azul.teletransportar(0, 0, 0);
        rojo.teletransportar(10, 0, 0); // 10 unidades de distancia

        float dist = azul.calcularDistancia(rojo);
        
        Narrador.obtenerInstancia().narrar("Distancia detectada: " + dist + " unidades.", 30);

        if (azul.percibir(rojo, 15.0f)) {
            azul.hablar("¡Te veo, Slime Rojo! Estás a mi alcance.");
        } else {
            azul.hablar("No huelo nada por aquí...");
        }
    }*/
}